// Esta es solo una PLANTILLA — no lleva credenciales reales y sí se sube a GitHub.
// El archivo real (config.js) ahora se genera automáticamente en cada despliegue
// de Vercel, usando las variables de entorno SUPABASE_URL y SUPABASE_ANON_KEY
// que configuras en Vercel > Settings > Environment Variables.
//
// Esta plantilla solo sirve como referencia de qué forma debe tener config.js,
// y para que, si alguna vez quieres probar el sitio abriendo index.html
// directamente en tu computador (sin pasar por Vercel), copies este archivo,
// lo renombres a config.js, y pegues tus credenciales reales ahí — sabiendo
// que ese config.js local nunca se sube a GitHub (está en .gitignore).

const SUPABASE_URL = "PEGA_AQUI_TU_PROJECT_URL";
const SUPABASE_ANON_KEY = "PEGA_AQUI_TU_ANON_KEY";
